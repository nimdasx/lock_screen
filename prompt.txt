You are an expert Flutter and Android developer.

Build a production-ready Flutter Android app whose only purpose is to lock the screen instantly with one tap. The app is intended for Android phones with a broken power button, so it must provide a simple software-based alternative to the physical power button.

Requirements:
- Flutter frontend.
- Android only.
- Very simple UI with one main action: a large “Lock Screen” button.
- When tapped, the device should lock immediately.
- The app should also be easy to launch from the home screen as a shortcut/icon.
- Keep the app lightweight, fast, and minimal.
- No login, no ads, no analytics, no unnecessary features.

Technical requirements:
- Use the correct Android approach for locking the screen.
- If device admin / device policy access is needed, implement it properly and explain the setup.
- Include all required Android configuration, permissions, and manifest changes.
- Make the code compatible with modern Android versions as much as possible.
- Handle errors gracefully if the device cannot be locked for any reason.
- Use clean project structure and clear comments.

Deliverables:
1. Full Flutter project source code.
2. Android native code/configuration needed for screen locking.
3. Any required manifest, permission, or admin receiver setup.
4. Steps to run, build, and test the app.
5. Instructions for creating or adding a home screen shortcut if needed.
6. A short explanation of any Android limitations.

Please provide:
- The complete code, not just a snippet.
- File-by-file structure.
- Clear setup instructions.
- Notes on how the locking mechanism works.
- If there are multiple implementation options, choose the most practical one for a real end-user app.